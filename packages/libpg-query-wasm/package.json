{
  "name": "libpg-query-wasm",
  "version": "0.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "type": "module",
  "exports": "./libpg-query.mjs",
  "scripts": {
    "build": "npm run clean && npm run build:libpg_query && npm run build:wasm",
    "build:libpg_query": "(cd libpg_query && sed -i.bak 's/AR = ar rs/AR = emar rs/' Makefile && emmake make build)",
    "build:wasm": "emcc --bind -o dist/lib.mjs -I libpg_query -L libpg_query -l pg_query lib.cpp",
    "clean": "rm -rf dist/* && (cd libpg_query && make clean)",
    "start": "node .",
    "test": "node --test test/"
  },
  "dependencies": {
    "protobufjs": "~7.2.5"
  },
  "devDependencies": {}
}
