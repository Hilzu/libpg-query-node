{
  "name": "libpg-query-node",
  "version": "0.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "format": "npm run prettier -- --write",
    "prettier": "prettier .",
    "test": "npm run test:format && npm run --workspaces test",
    "test:format": "npm run prettier -- --check"
  },
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "packages/libpg-query-wasm",
    "packages/prettier-plugin-postgresql"
  ],
  "dependencies": {},
  "devDependencies": {
    "prettier": "3.0.3"
  }
}
